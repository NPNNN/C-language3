#include<stdio.h>
int main()
{
    int a,b,c,d,e;
    printf("Enter the mark of five subject");
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&d);
    if(a&&b&&c&&d&&e>33)
    {
        printf("the student is passed");
    }
    else
    {
        printf("the studenr is failed");
    }
    return 0;
}